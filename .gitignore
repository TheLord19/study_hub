node_modules/
dist/

# your database — rebuilds itself from schema + seed on first start.
# Anchored with a leading slash: a bare `data/` would also swallow
# server/data/, which is the seed catalogue and has to be committed.
/data/

.env
.env.*
!.env.example

.vite/
*.local
*.log
npm-debug.log*

.DS_Store
Thumbs.db
.superdesign/
